<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1>Welcome to the mini message board</h1>

    <% for (let i= 0; i< message.length; i++) { %>
    <div class="eachMessage">
      <div>User: <%= message[i].user %></div>
      <div>Text: <%= message[i].text %></div>
      <div>Date: <%= message[i].added %></div>
    </div>
    <br />
    <% } %>
  </body>
</html>
